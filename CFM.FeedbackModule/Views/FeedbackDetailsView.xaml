<UserControl x:Class="CFM.FeedbackModule.Views.FeedbackDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CFM.FeedbackModule.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <Label>
                <TextBlock Text="{Binding CurrentFeedback.Description}" TextTrimming="WordEllipsis"></TextBlock>
            </Label>
            <Label Content="{Binding CurrentFeedback.Assignment.Title}"></Label>
            <Label Content="{Binding CurrentFeedback.Assignment.Unit.Title}"></Label>
            <ListView ItemsSource="{Binding CurrentFeedback.Assignment.Unit.Teachers}"
                      DisplayMemberPath="FullName"></ListView>
        </StackPanel>
    </Grid>
</UserControl>
